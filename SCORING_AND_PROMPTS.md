# 生成评分判断基准和Prompt说明

## 📊 生成评分判断基准

生成评分（`generator_score`）的计算逻辑位于 `app/core/generator.py` 的 `_calculate_generator_score` 方法。

### 评分规则（总分0-100分）

**基础分：50分**

#### 1. 问题长度检查（±10分）
- ✅ **10-200字符**：+10分
- ❌ **少于10字符**：-10分
- ⚠️ **超过200字符**：不加分也不扣分

#### 2. 答案长度检查（±20分）
- ✅ **50-2000字符**：+20分
- ❌ **少于50字符**：-20分
- ⚠️ **超过2000字符**：不加分也不扣分

#### 3. 问题格式检查（+10分）
- ✅ **包含问号**（`?` 或 `？`）：+10分

#### 4. 答案详细程度检查（+10分）
- ✅ **包含3个或以上句子**：+10分
  - 句子分隔符：`。！？.!?`

### 评分示例

**高分示例（90-100分）：**
- 问题：10-200字符，包含问号 ✅
- 答案：50-2000字符，包含3+句子 ✅
- 总分：50（基础）+ 10（问题长度）+ 20（答案长度）+ 10（问号）+ 10（详细度）= **100分**

**低分示例（30-50分）：**
- 问题：少于10字符，无问号 ❌
- 答案：少于50字符，少于3句 ❌
- 总分：50（基础）- 10（问题长度）- 20（答案长度）= **20分**

---

## 🤖 自动生成问题的Prompt

Prompt模板位于 `app/services/prompt_manager.py`，根据不同的知识领域使用不同的模板。

### Prompt模板结构

所有模板都包含以下参数：
- `{category}` - 知识领域（如：通用知识、技术流程等）
- `{topic}` - 主题关键词（可选，如果没有则显示"不限"）
- `{style}` - 生成风格（专业/通俗/详细）

### 各知识领域的Prompt模板

#### 1. 通用知识（GENERAL）

```
你是一個專業的問答生成助手。請根據以下要求生成一個問答對：

知識領域：{category}
主題關鍵詞：{topic}
生成風格：{style}

要求：
1. 問題應該清晰、具體，能夠準確表達用戶的需求
2. 答案應該準確、完整，能夠充分回答問題
3. 答案應該符合{style}風格
4. 如果提供了主題關鍵詞，問答應該圍繞該主題

請生成一個問答對，格式如下：
問題：[問題內容]
答案：[答案內容]
```

#### 2. 技术流程（TECHNICAL）

```
你是一個技術文檔專家。請根據以下要求生成一個技術問答對：

知識領域：{category}
主題關鍵詞：{topic}
生成風格：{style}

要求：
1. 問題應該針對技術流程、操作步驟或技術規範
2. 答案應該詳細說明技術步驟，包含必要的操作細節
3. 答案應該準確、專業，符合技術文檔標準
4. 如果涉及操作步驟，應該按順序列出

請生成一個問答對，格式如下：
問題：[問題內容]
答案：[答案內容]
```

#### 3. 故障排除（TROUBLESHOOTING）

```
你是一個故障排除專家。請根據以下要求生成一個故障排除問答對：

知識領域：{category}
主題關鍵詞：{topic}
生成風格：{style}

要求：
1. 問題應該描述一個常見的故障或問題場景
2. 答案應該提供清晰的解決步驟和方案
3. 答案應該包含問題原因分析和解決方法
4. 如果可能，應該提供預防措施

請生成一個問答對，格式如下：
問題：[問題內容]
答案：[答案內容]
```

#### 4. 资安法规（SECURITY）

```
你是一個資安和法規專家。請根據以下要求生成一個資安法規問答對：

知識領域：{category}
主題關鍵詞：{topic}
生成風格：{style}

要求：
1. 問題應該涉及安全規範、法規要求或合規性問題
2. 答案應該準確、嚴謹，符合法規要求
3. 答案應該包含相關的法規依據或標準
4. 答案應該強調安全性和合規性

請生成一個問答對，格式如下：
問題：[問題內容]
答案：[答案內容]
```

#### 5. 应用案例（CASE_STUDY）

```
你是一個案例研究專家。請根據以下要求生成一個應用場景問答對：

知識領域：{category}
主題關鍵詞：{topic}
生成風格：{style}

要求：
1. 問題應該描述一個實際應用場景或案例
2. 答案應該詳細說明應用場景、實施過程和效果
3. 答案應該包含具體的案例細節和經驗總結
4. 答案應該具有參考價值和實用性

請生成一個問答對，格式如下：
問題：[問題內容]
答案：[答案內容]
```

---

## 🔄 Prompt模板加载优先级

1. **数据库模板**（如果提供了 `template_id`）
2. **数据库活动模板**（该分类的最新活动模板）
3. **缓存模板**（内存中的模板）
4. **默认模板**（代码中定义的模板）
5. **通用模板**（如果以上都没有，使用通用知识模板）

---

## 📝 实际使用的Prompt示例

假设用户选择：
- 知识领域：通用知识
- 主题关键词：container相關資料
- 生成风格：专业

实际发送给AI的Prompt会是：

```
你是一個專業的問答生成助手。請根據以下要求生成一個問答對：

知識領域：通用知识
主題關鍵詞：container相關資料
生成風格：专业

要求：
1. 問題應該清晰、具體，能夠準確表達用戶的需求
2. 答案應該準確、完整，能夠充分回答問題
3. 答案應該符合专业風格
4. 如果提供了主題關鍵詞，問答應該圍繞該主題

請生成一個問答對，格式如下：
問題：[問題內容]
答案：[答案內容]
```

---

## 💡 优化建议

### 生成评分可以优化的方向：
1. **内容质量检查**：检查答案是否真正回答了问题
2. **专业性检查**：根据知识领域检查专业术语使用
3. **可读性检查**：检查答案的逻辑结构和可读性
4. **完整性检查**：检查答案是否完整回答了问题

### Prompt可以优化的方向：
1. **更详细的格式要求**：指定更严格的输出格式
2. **示例引导**：提供示例问答对作为参考
3. **质量要求**：明确质量标准和检查点
4. **领域特定要求**：根据不同领域添加特定要求

---

## 📍 代码位置

- **生成评分逻辑**：`app/core/generator.py` 第69-106行
- **Prompt模板管理**：`app/services/prompt_manager.py`
- **Prompt使用**：`app/core/generator.py` 第137-143行

