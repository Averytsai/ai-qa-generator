# 🔐 SECRET_KEY 说明

## 📋 什么是 SECRET_KEY？

`SECRET_KEY` 是一个**密钥字符串**，用于应用程序的加密和安全操作。

## 🎯 SECRET_KEY 的作用

### 1. 数据加密和签名

`SECRET_KEY` 用于：
- **JWT（JSON Web Token）签名**：生成和验证用户认证令牌
- **Session 加密**：加密用户会话数据
- **数据签名**：确保数据完整性，防止篡改
- **密码重置令牌**：生成安全的密码重置链接
- **CSRF 保护**：防止跨站请求伪造攻击

### 2. 安全性

**为什么需要 SECRET_KEY？**
- 确保敏感数据不会被未授权访问
- 防止数据被篡改
- 保护用户隐私
- 符合安全最佳实践

## 🔑 SECRET_KEY 的特点

### 要求

1. **随机性**：必须是随机生成的，不能是简单的字符串
2. **长度**：通常至少 32 个字符
3. **保密性**：绝对不能泄露给他人
4. **唯一性**：每个应用应该使用不同的 SECRET_KEY

### 格式

`SECRET_KEY` 通常是一个长字符串，例如：
```
yg2m6Uc_dB1zYfbj1ggCOJVBAoLQjnCITz-oqCHIDOw
```

## 📝 在你的项目中的使用

### 配置位置

在 `app/config.py` 中定义：

```python
secret_key: str = Field(..., description="密鑰（用於加密）")
```

**`Field(...)` 的含义：**
- `...` 表示这是**必需字段**
- 没有默认值
- 必须从环境变量中读取

### 为什么是必需的？

因为应用程序需要：
- 加密敏感数据
- 生成安全的令牌
- 保护用户会话
- 确保数据完整性

## 🛠️ 如何生成 SECRET_KEY？

### 方法 1: 使用项目提供的脚本（推荐）

```bash
python3 scripts/generate_secret_key.py
```

这会生成一个安全的随机密钥，例如：
```
yg2m6Uc_dB1zYfbj1ggCOJVBAoLQjnCITz-oqCHIDOw
```

### 方法 2: 使用 Python 命令行

```bash
python3 -c "import secrets; print(secrets.token_urlsafe(32))"
```

### 方法 3: 使用在线工具

可以使用在线密钥生成器，但**不推荐**，因为：
- 可能不安全
- 无法保证随机性
- 可能被记录

## 🔒 安全注意事项

### ✅ 应该做的

1. **使用随机生成的密钥**
2. **定期更换密钥**（如果可能）
3. **在生产环境中使用强密钥**
4. **将密钥存储在环境变量中**（不要写在代码里）
5. **不同环境使用不同的密钥**

### ❌ 不应该做的

1. **不要使用简单的字符串**（如 "password123"）
2. **不要将密钥提交到 Git**
3. **不要在不同应用间共享密钥**
4. **不要将密钥写在代码中**
5. **不要泄露密钥给他人**

## 📍 在你的项目中的配置

### 开发环境（本地）

在 `.env` 文件中设置：
```bash
SECRET_KEY=你的密钥
```

### 生产环境（Railway）

在 Railway Dashboard 中设置：
1. 进入后端服务
2. Settings → Variables
3. 添加变量：
   - **Name:** `SECRET_KEY`
   - **Value:** `yg2m6Uc_dB1zYfbj1ggCOJVBAoLQjnCITz-oqCHIDOw`

## 🔄 如果 SECRET_KEY 泄露了怎么办？

如果密钥泄露：
1. **立即更换密钥**
2. **重新生成所有令牌**
3. **检查是否有未授权访问**
4. **更新所有环境中的密钥**

## 💡 常见问题

### Q1: 我可以使用任何字符串作为 SECRET_KEY 吗？

**A:** 技术上可以，但**强烈不推荐**。应该使用随机生成的强密钥。

### Q2: SECRET_KEY 需要多长？

**A:** 建议至少 32 个字符。我们生成的密钥通常是 43 个字符。

### Q3: 我可以重复使用同一个 SECRET_KEY 吗？

**A:** 可以，但建议：
- 不同环境（开发/生产）使用不同的密钥
- 定期更换密钥（如果可能）

### Q4: 如果我不设置 SECRET_KEY 会怎样？

**A:** 应用程序会**无法启动**，因为：
- `SECRET_KEY` 是必需字段
- 没有默认值
- 会导致 `ValidationError`

### Q5: SECRET_KEY 和密码有什么区别？

**A:**
- **SECRET_KEY**：用于应用程序的加密和签名，不用于用户认证
- **密码**：用于用户登录认证

## 📋 总结

- **SECRET_KEY** 是应用程序的加密密钥
- 用于数据加密、签名和安全操作
- 必须是随机生成的强密钥
- 必须存储在环境变量中
- 绝对不能泄露

## 🚀 下一步

1. 生成一个 SECRET_KEY（使用 `scripts/generate_secret_key.py`）
2. 在 Railway Dashboard 中设置环境变量
3. 重新部署应用

