# 開發規則 (Development Rules)

## 1. 開發原則

### 1.1 漸進式開發
- **一次只開發一個功能模組**
- 每個模組完成後進行測試和驗證
- 確認無誤後再進行下一個模組
- 避免一次性修改多個檔案

### 1.2 模組化設計
- 每個功能應該獨立成模組
- 模組之間保持低耦合
- 使用清晰的介面定義模組間的互動

### 1.3 測試優先
- 每個功能開發前先定義測試案例
- 開發過程中持續進行單元測試
- 功能完成後進行整合測試

## 2. 開發流程

### 2.1 開發階段劃分
```
階段1: 基礎架構和配置
  - 專案結構設定
  - 依賴套件安裝
  - 基礎配置檔案

階段2: 核心功能模組
  - 資料處理模組
  - API 整合模組
  - 業務邏輯模組

階段3: 使用者介面
  - UI 元件開發
  - 使用者互動流程
  - 錯誤處理和提示

階段4: 整合和優化
  - 模組整合測試
  - 效能優化
  - 文件撰寫
```

### 2.2 開發檢查清單
每個模組開發完成後，檢查：
- [ ] 程式碼符合專案風格規範
- [ ] 通過所有相關測試
- [ ] 錯誤處理完善
- [ ] 註解和文件齊全
- [ ] 無 linter 錯誤
- [ ] 功能符合需求規格

## 3. 程式碼規範

### 3.1 命名規範
- **檔案名稱**: 使用小寫字母和底線，例如 `data_processor.py`
- **類別名稱**: 使用 PascalCase，例如 `DataProcessor`
- **函數/變數名稱**: 使用 snake_case，例如 `process_data()`
- **常數名稱**: 使用大寫字母和底線，例如 `MAX_RETRY_COUNT`

### 3.2 程式碼結構
```python
# 1. 標準庫導入
import os
import sys

# 2. 第三方庫導入
import requests
import pandas as pd

# 3. 本地模組導入
from .utils import helper_function

# 4. 常數定義
DEFAULT_TIMEOUT = 30

# 5. 類別定義
class MyClass:
    """類別說明"""
    pass

# 6. 函數定義
def my_function():
    """函數說明"""
    pass

# 7. 主程式入口
if __name__ == "__main__":
    main()
```

### 3.3 註解規範
- 每個模組、類別、函數都應該有 docstring
- 複雜邏輯需要行內註解說明
- 使用中文註解（如果專案是中文專案）

## 4. 錯誤處理

### 4.1 異常處理原則
- 明確捕獲特定異常類型
- 提供有意義的錯誤訊息
- 記錄錯誤日誌
- 優雅降級處理

### 4.2 範例
```python
try:
    result = process_data(data)
except ValueError as e:
    logger.error(f"資料格式錯誤: {e}")
    raise
except ConnectionError as e:
    logger.error(f"連線錯誤: {e}")
    return default_value
except Exception as e:
    logger.error(f"未預期的錯誤: {e}")
    raise
```

## 5. 版本控制

### 5.1 Git 提交規範
- 每次提交只包含一個功能的變更
- 提交訊息格式: `[類型] 簡短描述`
  - `[feat]`: 新功能
  - `[fix]`: 修復 bug
  - `[refactor]`: 重構
  - `[docs]`: 文件更新
  - `[test]`: 測試相關

### 5.2 分支策略
- `main`: 穩定版本
- `develop`: 開發分支
- `feature/功能名稱`: 功能分支

## 6. 測試規範

### 6.1 測試覆蓋率
- 核心功能測試覆蓋率 > 80%
- 關鍵業務邏輯測試覆蓋率 > 90%

### 6.2 測試類型
- **單元測試**: 測試單一函數或類別
- **整合測試**: 測試模組間的互動
- **端對端測試**: 測試完整流程

## 7. 文件規範

### 7.1 必要文件
- README.md: 專案說明和快速開始
- API.md: API 文件（如有）
- CHANGELOG.md: 版本變更記錄

### 7.2 程式碼文件
- 每個公開函數/類別都需要 docstring
- 複雜演算法需要說明文件

## 8. 效能考量

### 8.1 優化原則
- 先確保功能正確，再優化效能
- 使用適當的資料結構
- 避免不必要的資料庫查詢或 API 呼叫
- 考慮快取機制

### 8.2 監控指標
- 回應時間
- 記憶體使用
- CPU 使用率
- 錯誤率

## 9. 安全性

### 9.1 安全原則
- 驗證所有使用者輸入
- 使用參數化查詢（避免 SQL 注入）
- 敏感資訊使用環境變數
- 定期更新依賴套件

## 10. 開發工具

### 10.1 推薦工具
- **Linter**: pylint, flake8, black
- **測試框架**: pytest
- **型別檢查**: mypy
- **格式化**: black, isort

### 10.2 設定檔
- `.pylintrc`: pylint 設定
- `pyproject.toml`: 專案設定和工具配置
- `.pre-commit-config.yaml`: Git hooks 設定

